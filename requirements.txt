numpy>=1.24
PyYAML>=6.0
jsonschema>=4.21
typing-extensions>=4.10
msgpack>=1.0
simplejson>=3.19

# Dev (optional)
# mypy>=1.9
# ruff>=0.4
# pytest>=8.0
# pytest-cov>=5.0

# ROS Python deps are provided by your ROS distro (install via apt), e.g.:
# sudo apt install ros-<distro>-rospy ros-<distro>-sensor-msgs ros-<distro>-std-msgs
